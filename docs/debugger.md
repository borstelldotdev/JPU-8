# Debugger

Debuggaren låter dig hitta fel i ditt program.
För att använda debuggaren, placera breakpoints i din kod med instruktionen `SYS PAUSE`.
Kör sedan programmet med `-d`, `--debug` eller `--debugger`.
När programmet når en breakpoint kan du då gå in i REPL-en.